export {
  type Platform,
  StreamClient,
  type StreamClientOptions,
} from "@api/stream/client.ts";

export type {
  StreamGetChannelResponse,
  StreamGetChannelOptions,
} from "@api/stream/endpoints/get-channel.ts";

export type {
  StreamGetLiveListResponse,
  StreamGetLiveListOptions,
} from "@api/stream/endpoints/get-live-list.ts";

export type {
  StreamSearchChannelResponse,
  StreamSearchChannelOptions,
} from "@api/stream/endpoints/search-channel.ts";

export type {
  StreamSearchLiveResponse,
  StreamSearchLiveOptions,
} from "@api/stream/endpoints/search-live.ts";

export type {
  StreamSearchTagResponse,
  StreamSearchTagOptions,
} from "@api/stream/endpoints/search-tag.ts";
